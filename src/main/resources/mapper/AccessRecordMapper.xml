<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.AccessRecordDao">

    <select id="getAccessRecordByDepartment" resultType="AccessRecord">
        select * from accessrecord where department = #{department} and userid = #{userid}
    </select>

    <select id="getAccessRecordById" resultType="AccessRecord">
        select * from accessrecord where id = #{id}
    </select>

    <insert id="insertAccessRecord" parameterType="AccessRecord" >
        insert into accessrecord (name, content, priority, date, userid,department)
        values (#{name},#{content},#{priority},#{date},#{userid},#{department})
    </insert>

</mapper>