<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.DoctorDao">


    <select id="getDoctorByName" resultType="Doctor">
        select * from `doctor` where name=#{name}
    </select>

    <insert id="insertDoc" parameterType="Doctor" >
        insert into doctor (name, password, department)
        values (#{name}, #{password}, #{department})
    </insert>

    <delete id="deleteDoc" parameterType="Doctor">
        delete from doctor where name = #{name}
    </delete>

</mapper>

